# Changelog

All notable changes per `VERSION` bump. One section per release. CHANGELOG entries are part of step 2 of the `code!` 5-step workflow (alongside the `VERSION` bump) — they ride in the same commit as the version bump and the code change.

Format: `## v<X.Y.Z> — YYYY-MM-DD` followed by bullets, optionally grouped by area.

---

## v0.1.0 — <YYYY-MM-DD>

Initial scaffold from PROJECT_STARTER template v1.0.0. No source code yet.
